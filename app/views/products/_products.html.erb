<% @products.each do |product| %>
<div class="image_wrapper hastip"  title="<%=product.title%>">
<%= image_tag product.image_src , :class => "main_images" %>

<div class="image_info">
<span class="title" ><%= truncate(product.title, length: 18) %></span>
</div>

	<div style="" class="button_wrapper">
    <a style="color:#fff;font-weight:bold;" class="button tiny pos" data-product-id = "<%= product.id %>" href="#">POS</a>
  	</div>

</div>
<% end %>
